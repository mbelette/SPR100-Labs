Observation: All tasks in the threading script had the same PID because threads share the same process resources

Monitor: The process monitor successfully showed CPU and Memory usage for active system processes
